"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var CommandsManager = (function () {
    function CommandsManager(module) {
        this.module = module;
    }
    CommandsManager.prototype.HandleComamnds = function (msg) {
        var _this = this;
        if (msg.author.id === this.module.getClient().user.id)
            return;
        var args = msg.content.split(" ");
        if (args[0].indexOf(this.module.moduleOptions.config.prefix) < 0)
            return;
        this.module.moduleOptions.commands.forEach(function (cmd) {
            if (args[0].substring(_this.module.moduleOptions.config.prefix.length) === cmd.name) {
                new cmd.class(_this.module.getClient(), msg).run();
                return;
            }
        });
    };
    return CommandsManager;
}());
exports.CommandsManager = CommandsManager;
//# sourceMappingURL=CommandsManager.js.map