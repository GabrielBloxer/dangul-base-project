import { Module } from "../types/Module";
import { ModuleOptions } from "../types/ModuleOptions";

export function DtModule(mOptions: ModuleOptions) {
    return (target: any) => {
        let nT: any = new target(mOptions);

        nT.initBot();

        nT.onInit();

        return nT;
    }
}