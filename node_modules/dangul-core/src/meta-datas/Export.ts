import { Module } from "../types/Module";

export function ExportTo(module: Module) {
    return (target: any) => {
        if (target.coman === true) {
            if (module.moduleOptions.commands.indexOf(target) > -1) return;

            module.moduleOptions.commands.push(target);
        }
    };
}